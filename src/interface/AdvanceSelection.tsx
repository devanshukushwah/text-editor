export default interface AdvanceSelection {
  ignoreCase: boolean;
  wholeWord: boolean;
  includeLowerCase: boolean;
  includeUpperCase: boolean;
  includeNumbers: boolean;
  startWith: string;
  endWith: string;
}
