export default interface RegexProps {
  ignoreCase?: boolean;
  wholeWord?: boolean;
  includeLowerCase?: boolean;
  includeUpperCase?: boolean;
  startWith?: string;
  endWith?: string;
  includeNumbers?: boolean;
}
